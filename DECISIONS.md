# DECISIONS — autonomous hardening pass (2026-05-26)

Running log of non-obvious decisions made while working autonomously, so they
can be reviewed afterwards.

## Process

- **Worktree-isolated agents.** Parallel work runs in separate git worktrees on
  dedicated branches to avoid file contention; the lead integrates by merging PRs
  one at a time and keeping the CI matrix green. Rationale: respects the
  "no two agents on the same file simultaneously" rule and the "never merge
  untested code to main" rule.
- **No tags / no release / no publish.** Per the safety guardrails the lead brings
  `main` to a release-ready, fully green state and only *recommends* the release in
  FINAL_REPORT.md. Tagging is left to the maintainer.

## PRs

- **#49 / #50 (dependabot GH-Actions bumps):** both fully green on their last CI run
  including `windows-latest, 3.12`. The bumps are CI-action-only (no Python code),
  so the green matrix is authoritative — **merged (squash)** 2026-05-26;
  `main` advanced 4fbfce1 → 24bc15b → **merge (squash)**.

## PR integration log

- **PR #51** (`--chrome-executable`, Phase 2): full matrix green incl.
  `windows-latest 3.12` → **merged (squash)** into `main` (d200dc6). Closes #39.
- **PR #52** (cross-platform robustness, Phase 3): Linux + macOS green, but
  **`windows-latest 3.12` failed** — the new `_ensure_systemd_available()` guard
  (`shutil.which("systemctl")`) made 3 *pre-existing* `test_service.py` install/
  uninstall tests raise `ServiceNotAvailableError` on Windows (no systemctl),
  invisible on the Linux dev box. Exactly the CLAUDE.md Windows trap. Held the
  merge; sent the precise root-cause + fix (mock `service.shutil.which`) back to
  the platform agent. *Not merged until Windows is green.*

## Phase 6 — branch cleanup analysis (executed only after PRs merge)

- `claude/analyze-project-i5gvf`: **0 commits ahead** of `main` → empty/merged →
  delete (local + origin).
- `chore/screenshots-25447672277`: 1 commit with 5 PNG screenshots that are **not
  in `main`**, **not referenced** anywhere in `docs/`/`README`, and generated by a
  `screenshots.yml` workflow that **no longer exists** in `main`. Fully orphaned
  auto-generated branch → safe to delete (no content loss of anything live).
- Merged dependabot branches for #49/#50 were auto-deleted on merge.

**Executed 2026-05-26 (after all PRs merged + main verified green):** deleted on
origin *and* locally — `harden/cross-platform`, `audit/api-coverage`,
`ux/diagnostics-polish`, `fix/openconnect-missing-binary`,
`fix/redact-session-token` (all merged via PRs #52–#56), plus the orphaned
`chore/screenshots-25447672277` and `claude/analyze-project-i5gvf`. Removed the
agent worktrees and `git fetch --prune`d. Result: `main` is the only branch
locally and on origin.

## Issue decisions

- **#39** — auto-closed by PR #51 (`Closes #39`): `--chrome-executable` is the
  promised fix.
- **#24** — **closed as resolved** with a full explanation comment. Rationale:
  the slot-signature crash was fixed in v0.21.0; the remaining "LED never blinks"
  is the WebUSB-stripped-at-compile-time property of the PyPI `PyQt6-WebEngine`
  wheel (a Qt build limitation, not fixable in a Python wrapper) and is
  documented; the practical `--browser chrome` path is now frictionless for
  distro-`chromium` users via the new `--chrome-executable`. Invited reopen with
  DEBUG logs if a hardware key still fails via chrome-mode.

## Issues

- **#24:** the code crash (slot signature) was already fixed in v0.21.0; the WebUSB
  limitation is a Qt-build property of PyPI wheels and out of scope for a Python
  wrapper. The genuinely actionable, maintainer-promised remainder is the
  `--chrome-executable` flag (shared with #39). We therefore drive #24 forward via
  that feature and the existing documented `--browser chrome` guidance rather than
  attempting an impossible qt-mode WebUSB fix.
- **#39:** root fix is `--chrome-executable PATH` → Playwright `executable_path=`,
  letting a system `chromium` be used directly. `--chrome-channel chromium` cannot
  solve it (Playwright has no `chromium` channel). → **PR #51**
  `feat(chrome): add --chrome-executable …` (`Closes #39`). Also added the
  missing `--chrome-channel` completion entries while there. Local: 955
  passed / 4 skipped, coverage 76.17 %, ruff clean. Awaiting CI matrix.
